name: 24/7 Day-Trader News Bot

on:
  schedule:
    # Runs automatically every 5 hours to keep the bot alive 24/7
    # (The Python script will automatically exit at 4 hours & 55 mins to prevent GitHub timeout errors)
    - cron: '0 */5 * * *'
  workflow_dispatch: # Adds a button so you can manually start it from the GitHub website

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Install Playwright Browsers
        run: |
          playwright install chromium

      - name: Run News Engine
        env:
          FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python news_engine.py
